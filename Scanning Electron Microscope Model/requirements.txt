opencv-python>=4.8
numpy>=1.24
Pillow>=10.0
openpyxl>=3.1
torch>=2.0
torchvision>=0.15
ultralytics>=8.1

# 若无写 conda 权限，可安装到本地:
#   pip install ultralytics --target .pylibs --no-deps
#   pip install matplotlib pandas pyyaml requests scipy seaborn tqdm psutil py-cpuinfo --target .pylibs
# 然后: set PYTHONPATH=.pylibs
